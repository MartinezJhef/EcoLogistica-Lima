# 11. Base de Datos

## 1. Datos generales

| Campo | Información |
|---|---|
| **Proyecto** | EcoLogística Lima – Optimizador de Rutas Sostenibles para DistriRápido S.A.C. |
| **Código del Proyecto** | PFA-ECOLIMA-2026 |
| **Responsable** | Jheferson Martinez Valerio |
| **Integrantes del Equipo** | • Zayuri Cerron Medina (Directora de Proyecto)<br>• Jheferson Martinez Valerio (Líder Metodológico / Scrum Master)<br>• Angela Rojas Quispe (Product Owner)<br>• Maylit Mendoza Alarcon (Analista de Riesgos / QA)<br>• Diego Angulo Gonzales (Gestor de Stakeholders y Comunicaciones) |
| **Fecha** | 17/09/2026 |
| **Versión** | 1.1.0 |
| **Estado** | Aprobado |

---

## 2. Modelo Conceptual — Diagrama Entidad-Relación

El modelo conceptual define las entidades de información requeridas para sustentar el cálculo de rutas óptimas de bajas emisiones de CO2, la gestión de flotas, la trazabilidad de pedidos con ventanas de tiempo y el control geoespacial en Lima Metropolitana.

### Entidades principales

| Entidad | Descripción |
|---|---|
| **USUARIO** | Credenciales, roles de acceso (RBAC) y estado de los actores del sistema (Administrador, Despachador, Conductor, Auditor). |
| **CONDUCTOR** | Perfil operativo asociado a un usuario, con número de licencia de conducir, teléfono y disponibilidad de turno. |
| **VEHÍCULO** | Datos técnicos de la flota de DistriRápido S.A.C. (placa, capacidad en kg/m³, tipo de combustible y factor de emisión). |
| **DEPÓSITO** | Punto origen/retorno de las rutas logísticas (almacenes centrales o hubs urbanos con coordenadas geográficas). |
| **PEDIDO** | Carga solicitada por un cliente que requiere entrega con ventana de tiempo estricta, peso, volumen y ubicación espacial. |
| **RUTA** | Plan de viaje generado por el motor de optimización (CVRPTW) asignado a un vehículo y conductor en una fecha dada. |
| **TRAMO** | Segmento vial secuencial entre dos puntos geográficos dentro de una ruta (distancia, tiempo de tránsito y CO2). |
| **ZONA_RESTRINGIDA** | Polígonos geográficos de Lima con restricciones de circulación vehicular, obras viales o zonas de bajo nivel de emisión. |
| **INCIDENCIA** | Eventos atípicos reportados en ruta por el conductor a través de la app móvil (tráfico, accidentes, cliente ausente). |
| **REPORTE_EMISIONES** | Consolidado periódico de métricas de sostenibilidad, consumo de combustible y huella de carbono mitigada (ISO 14083). |

### Relaciones principales

- Un **USUARIO** posee opcionalmente un perfil de **CONDUCTOR** (1:1 opcional).
- Un **CONDUCTOR** ejecuta cero o múltiples **RUTAS** a lo largo del tiempo (1:N).
- Un **VEHÍCULO** es asignado a cero o múltiples **RUTAS** diarias (1:N).
- Un **DEPÓSITO** actúa como punto de salida y llegada para múltiples **RUTAS** (1:N).
- Una **RUTA** se compone de uno o más **TRAMOS** de viaje ordenados (1:N).
- Una **RUTA** atiende múltiples **PEDIDOS** y un **PEDIDO** pertenece a una única **RUTA** activa (1:N).
- Un **PEDIDO** genera cero o múltiples **INCIDENCIAS** durante la entrega (1:N).
- Múltiples **RUTAS** alimentan la generación de un **REPORTE_EMISIONES** (N:M a través de la tabla **DETALLE_REPORTE_RUTA**).
- Los **TRAMOS** pueden cruzar o evitar **ZONAS_RESTRINGIDAS** mediante consultas de intersección geoespacial (relación implícita por motor espacial).

```mermaid
erDiagram
    USUARIO ||--o| CONDUCTOR : "posee perfil"
    CONDUCTOR ||--o{ RUTA : "ejecuta"
    VEHICULO ||--o{ RUTA : "asignado a"
    DEPOSITO ||--o{ RUTA : "origen y retorno"
    RUTA ||--|{ TRAMO : "contiene"
    RUTA ||--o{ PEDIDO : "planifica"
    PEDIDO ||--o{ INCIDENCIA : "registra"
    ZONA_RESTRINGIDA }o--o{ TRAMO : "evita"
    RUTA }|--|{ REPORTE_EMISIONES : "genera (DETALLE_REPORTE_RUTA)"

    USUARIO {
        uuid usuario_id PK
        string email
        string rol
        string estado
    }
    CONDUCTOR {
        uuid conductor_id PK
        uuid usuario_id FK
        string licencia
        string estado
    }
    VEHICULO {
        uuid vehiculo_id PK
        string placa
        decimal capacidad_peso_kg
        string tipo_combustible
    }
    DEPOSITO {
        uuid deposito_id PK
        string nombre
        geometry ubicacion_geografica
    }
    PEDIDO {
        uuid pedido_id PK
        string codigo_seguimiento
        geometry ubicacion_destino
        time ventana_inicio
        time ventana_fin
    }
    RUTA {
        uuid ruta_id PK
        uuid deposito_origen_id FK
        uuid conductor_id FK
        uuid vehiculo_id FK
        date fecha_operacion
    }
    TRAMO {
        uuid tramo_id PK
        uuid ruta_id FK
        int orden_secuencia
        geometry geometria_linea
    }
    ZONA_RESTRINGIDA {
        uuid zona_id PK
        string nombre
        geometry poligono_geografico
    }
    INCIDENCIA {
        uuid incidencia_id PK
        uuid pedido_id FK
        uuid conductor_id FK
        string tipo_incidencia
    }
    REPORTE_EMISIONES {
        uuid reporte_id PK
        date fecha_inicio_periodo
        date fecha_fin_periodo
        decimal total_co2_emitido_kg
    }

-- =============================================================================
-- EcoLogística Lima (PFA-ECOLIMA-2026)
-- Script DDL de Base de Datos - Versión 1.1.0
-- Motor Target: PostgreSQL 16+ con Extensión PostGIS
-- Nivel de Normalización: 3FN
-- =============================================================================

-- 1. Habilitación de Extensiones Requeridas
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "postgis";

-- -----------------------------------------------------------------------------
-- TABLA: usuarios
-- -----------------------------------------------------------------------------
CREATE TABLE usuarios (
    usuario_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    rol VARCHAR(20) NOT NULL CHECK (rol IN ('ADMIN', 'OPERADOR', 'CONDUCTOR', 'AUDITOR')),
    estado VARCHAR(20) NOT NULL DEFAULT 'ACTIVO' CHECK (estado IN ('ACTIVO', 'INACTIVO', 'BLOQUEADO')),
    creado_en TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_usuarios_email ON usuarios(email);
CREATE INDEX idx_usuarios_rol ON usuarios(rol);

-- -----------------------------------------------------------------------------
-- TABLA: conductores
-- -----------------------------------------------------------------------------
CREATE TABLE conductores (
    conductor_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    usuario_id UUID NOT NULL UNIQUE REFERENCES usuarios(usuario_id) ON DELETE CASCADE,
    nombres VARCHAR(100) NOT NULL,
    apellidos VARCHAR(100) NOT NULL,
    licencia VARCHAR(20) NOT NULL UNIQUE,
    telefono VARCHAR(15) NOT NULL,
    estado VARCHAR(20) NOT NULL DEFAULT 'DISPONIBLE' CHECK (estado IN ('DISPONIBLE', 'EN_RUTA', 'LICENCIA'))
);

CREATE INDEX idx_conductores_usuario ON conductores(usuario_id);
CREATE INDEX idx_conductores_estado ON conductores(estado);

-- -----------------------------------------------------------------------------
-- TABLA: vehiculos
-- -----------------------------------------------------------------------------
CREATE TABLE vehiculos (
    vehiculo_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    placa VARCHAR(10) NOT NULL UNIQUE,
    marca_modelo VARCHAR(100) NOT NULL,
    capacidad_peso_kg DECIMAL(10,2) NOT NULL CHECK (capacidad_peso_kg > 0),
    capacidad_volumen_m3 DECIMAL(10,2) NOT NULL CHECK (capacidad_volumen_m3 > 0),
    tipo_combustible VARCHAR(30) NOT NULL CHECK (tipo_combustible IN ('DIESEL', 'GASOLINA', 'GNV', 'ELECTRICO')),
    factor_emision_co2 DECIMAL(8,4) NOT NULL CHECK (factor_emision_co2 > 0),
    estado VARCHAR(20) NOT NULL DEFAULT 'DISPONIBLE' CHECK (estado IN ('DISPONIBLE', 'EN_RUTA', 'MANTENIMIENTO'))
);

CREATE INDEX idx_vehiculos_placa ON vehiculos(placa);
CREATE INDEX idx_vehiculos_estado ON vehiculos(estado);

-- -----------------------------------------------------------------------------
-- TABLA: depositos
-- -----------------------------------------------------------------------------
CREATE TABLE depositos (
    deposito_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    nombre VARCHAR(150) NOT NULL,
    direccion TEXT NOT NULL,
    ubicacion_geografica GEOMETRY(Point, 4326) NOT NULL,
    estado VARCHAR(20) NOT NULL DEFAULT 'OPERATIVO' CHECK (estado IN ('OPERATIVO', 'INACTIVO'))
);

CREATE INDEX idx_depositos_gist ON depositos USING GIST(ubicacion_geografica);

-- -----------------------------------------------------------------------------
-- TABLA: pedidos
-- -----------------------------------------------------------------------------
CREATE TABLE pedidos (
    pedido_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    codigo_seguimiento VARCHAR(30) NOT NULL UNIQUE,
    cliente_nombre VARCHAR(200) NOT NULL,
    direccion_destino TEXT NOT NULL,
    ubicacion_destino GEOMETRY(Point, 4326) NOT NULL,
    peso_kg DECIMAL(10,2) NOT NULL CHECK (peso_kg > 0),
    volumen_m3 DECIMAL(10,2) NOT NULL CHECK (volumen_m3 > 0),
    ventana_inicio TIME NOT NULL,
    ventana_fin TIME NOT NULL,
    prioridad VARCHAR(20) NOT NULL DEFAULT 'ESTANDAR' CHECK (prioridad IN ('ALTA', 'ESTANDAR', 'BAJA')),
    estado VARCHAR(20) NOT NULL DEFAULT 'PENDIENTE' CHECK (estado IN ('PENDIENTE', 'ASIGNADO', 'EN_RUTA', 'ENTREGADO', 'FALLIDO')),
    creado_en TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT chk_ventana_tiempo CHECK (ventana_fin > ventana_inicio)
);

CREATE INDEX idx_pedidos_estado ON pedidos(estado);
CREATE INDEX idx_pedidos_codigo ON pedidos(codigo_seguimiento);
CREATE INDEX idx_pedidos_gist ON pedidos USING GIST(ubicacion_destino);

-- -----------------------------------------------------------------------------
-- TABLA: rutas
-- -----------------------------------------------------------------------------
CREATE TABLE rutas (
    ruta_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    deposito_origen_id UUID NOT NULL REFERENCES depositos(deposito_id),
    conductor_id UUID NOT NULL REFERENCES conductores(conductor_id),
    vehiculo_id UUID NOT NULL REFERENCES vehiculos(vehiculo_id),
    fecha_operacion DATE NOT NULL,
    distancia_total_km DECIMAL(10,2) DEFAULT 0.00,
    tiempo_total_min INTEGER DEFAULT 0,
    co2_total_kg DECIMAL(10,2) DEFAULT 0.00,
    estado VARCHAR(20) NOT NULL DEFAULT 'PLANIFICADA' CHECK (estado IN ('PLANIFICADA', 'EN_PROCESO', 'FINALIZADA', 'CANCELADA')),
    creada_en TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_rutas_fecha ON rutas(fecha_operacion);
CREATE INDEX idx_rutas_conductor ON rutas(conductor_id);
CREATE INDEX idx_rutas_vehiculo ON rutas(vehiculo_id);

-- -----------------------------------------------------------------------------
-- TABLA: tramos
-- -----------------------------------------------------------------------------
CREATE TABLE tramos (
    tramo_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    ruta_id UUID NOT NULL REFERENCES rutas(ruta_id) ON DELETE CASCADE,
    orden_secuencia INTEGER NOT NULL CHECK (orden_secuencia > 0),
    origen_geometria GEOMETRY(Point, 4326) NOT NULL,
    destino_geometria GEOMETRY(Point, 4326) NOT NULL,
    distancia_tramo_km DECIMAL(8,2) NOT NULL,
    tiempo_estimado_min DECIMAL(8,2) NOT NULL,
    co2_tramo_kg DECIMAL(8,2) NOT NULL,
    geometria_linea GEOMETRY(LineString, 4326),
    CONSTRAINT uk_ruta_secuencia UNIQUE (ruta_id, orden_secuencia)
);

CREATE INDEX idx_tramos_ruta ON tramos(ruta_id);
CREATE INDEX idx_tramos_linea_gist ON tramos USING GIST(geometria_linea);

-- -----------------------------------------------------------------------------
-- TABLA: zonas_restringidas
-- -----------------------------------------------------------------------------
CREATE TABLE zonas_restringidas (
    zona_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    nombre VARCHAR(150) NOT NULL,
    motivo VARCHAR(100) NOT NULL,
    poligono_geografico GEOMETRY(Polygon, 4326) NOT NULL,
    hora_inicio_restriccion TIME,
    hora_fin_restriccion TIME,
    activa BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE INDEX idx_zonas_poligono_gist ON zonas_restringidas USING GIST(poligono_geografico);

-- -----------------------------------------------------------------------------
-- TABLA: incidencias
-- -----------------------------------------------------------------------------
CREATE TABLE incidencias (
    incidencia_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pedido_id UUID NOT NULL REFERENCES pedidos(pedido_id),
    conductor_id UUID NOT NULL REFERENCES conductores(conductor_id),
    tipo_incidencia VARCHAR(50) NOT NULL CHECK (tipo_incidencia IN ('TRAFICO_EXCESIVO', 'CLIENTE_AUSENTE', 'VEHICULO_AVERIA', 'CLIMA', 'OTRO')),
    descripcion TEXT,
    ubicacion_reporte GEOMETRY(Point, 4326),
    reportada_en TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_incidencias_pedido ON incidencias(pedido_id);
CREATE INDEX idx_incidencias_conductor ON incidencias(conductor_id);

-- -----------------------------------------------------------------------------
-- TABLA: reportes_emisiones
-- -----------------------------------------------------------------------------
CREATE TABLE reportes_emisiones (
    reporte_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    titulo VARCHAR(200) NOT NULL,
    fecha_inicio_periodo DATE NOT NULL,
    fecha_fin_periodo DATE NOT NULL,
    total_co2_emitido_kg DECIMAL(12,2) NOT NULL,
    total_co2_ahorrado_kg DECIMAL(12,2) NOT NULL,
    generado_por_usuario UUID NOT NULL REFERENCES usuarios(usuario_id),
    generado_en TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT chk_periodo_reporte CHECK (fecha_fin_periodo >= fecha_inicio_periodo)
);

-- -----------------------------------------------------------------------------
-- TABLA INTERMEDIA: detalle_reporte_ruta (Relación N:M)
-- -----------------------------------------------------------------------------
CREATE TABLE detalle_reporte_ruta (
    reporte_id UUID NOT NULL REFERENCES reportes_emisiones(reporte_id) ON DELETE CASCADE,
    ruta_id UUID NOT NULL REFERENCES rutas(ruta_id) ON DELETE CASCADE,
    PRIMARY KEY (reporte_id, ruta_id)
);